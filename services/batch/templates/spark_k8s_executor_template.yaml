apiVersion: v1
kind: Pod
metadata:
  name: spark-executor-minimal
  labels:
    spark-role: executor
spec:
  securityContext:
    runAsUser: 185
    runAsGroup: 185
    fsGroup: 185
  serviceAccountName: airflow-worker
  restartPolicy: Never
  containers:
    - name: spark-kubernetes
      imagePullPolicy: IfNotPresent
      resources:
        requests:
          memory: "512Mi"
          cpu: "500m"
        limits:
          memory: "1Gi"
          cpu: "1"
