apiVersion: v1
kind: PersistentVolume
metadata:
  name: datawarehouse-pv
  labels:
    type: local
spec:
  storageClassName: manual-datawarehouse
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/volume_sgp1_01/datawarehouse-data"
  # use node 1
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - big-data

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datawarehouse-pvc
  namespace: batch
spec:
  storageClassName: manual-datawarehouse
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: datawarehouse
  namespace: batch
  labels:
    app: datawarehouse
spec:
  replicas: 1
  strategy:
    type: Recreate # Avoid lock file issues with ReadWriteOnce/HostPath
  selector:
    matchLabels:
      app: datawarehouse
  template:
    metadata:
      labels:
        app: datawarehouse
    spec:
      # use node 1
      nodeSelector:
        node-role.kubernetes.io/manager: "true"

      # Security Context (User 1000)
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        runAsNonRoot: true

      containers:
        - name: postgresql
          image: debezium/postgres:13
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "warehousedb"
            - name: POSTGRES_USER
              value: "admin"
            - name: POSTGRES_PASSWORD
              value: "admin123"
            - name: POSTGRES_INITDB_ARGS
              value: "--auth-host=scram-sha-256"
          
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "1Gi"
              cpu: "500m"

          volumeMounts:
            - name: postgresql-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgresql-data
          persistentVolumeClaim:
            claimName: datawarehouse-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: datawarehouse
  namespace: batch
spec:
  type: NodePort
  selector:
    app: datawarehouse
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30055
